// Consultation fee (in INR) charged at the time of online booking.
// Client-safe: this module has no server-only imports, so it can be
// imported from both the browser (BookingModal) and the API routes.
export const CONSULTATION_FEE_INR = 400;
